apiVersion: 1
providers:
  - name: "Taller"
    orgId: 1
    folder: "Taller Observabilidad"
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
